{% extends "base.html.twig" %}

{% block body %}
       
{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="mb-4">
        {{ include("partials/_dynamic_collection_type.html.twig", {
            collectionType: form.screeningFormats,
            addButtonText: "Add Screening Format"
        }) }}
    </div>
{{ form_end(form) }}
   

{% endblock %}
